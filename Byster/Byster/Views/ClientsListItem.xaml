<UserControl
    x:Class="Byster.Views.ClientsListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Byster.Views"
    xmlns:l="clr-namespace:Byster.Models.ViewModels"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <l:ClientImageSourceConverter
                x:Key="iconToSource" />

            <Style
                x:Key="FocusVisual">
                <Setter
                    Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle
                                Margin="2"
                                StrokeDashArray="1 2"
                                Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                SnapsToDevicePixels="true"
                                StrokeThickness="1" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <SolidColorBrush
                x:Key="Button.Static.Background"
                Color="Transparent" />
            <SolidColorBrush
                x:Key="Button.MouseOver.Background"
                Color="#10FFFFFF" />
            <Style
                x:Key="ButtonRemoveStyle"
                TargetType="{x:Type Button}">
                <Setter
                    Property="FocusVisualStyle"
                    Value="{StaticResource FocusVisual}" />
                <Setter
                    Property="Background"
                    Value="{StaticResource Button.Static.Background}" />
                <Setter
                    Property="Foreground"
                    Value="White" />
                <Setter
                    Property="Cursor"
                    Value="Hand" />
                <Setter
                    Property="HorizontalContentAlignment"
                    Value="Center" />
                <Setter
                    Property="VerticalContentAlignment"
                    Value="Center" />
                <Setter
                    Property="Padding"
                    Value="1" />
                <Setter
                    Property="Template">
                    <Setter.Value>
                        <ControlTemplate
                            TargetType="{x:Type Button}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                SnapsToDevicePixels="true">
                                <Image
                                    Margin="3"
                                    x:Name="removeBox"
                                    Source="../Resources/Images/remove-icon.png"
                                    RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger
                                    Property="IsDefaulted"
                                    Value="true">
                                    <Setter
                                        Property="BorderBrush"
                                        TargetName="border"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                </Trigger>
                                <Trigger
                                    Property="IsMouseOver"
                                    Value="true">
                                    <Setter
                                        Property="Background"
                                        TargetName="border"
                                        Value="{StaticResource Button.MouseOver.Background}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>

    </UserControl.Resources>
    <Grid
        x:Name="gridMain"
        Cursor="Hand" >
        <Rectangle
            x:Name="background"
            Fill="White"
            Grid.ColumnSpan="3"
            Opacity="0.02"
            RadiusX="5"
            RadiusY="5" />

        <Grid
            Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="40" />
                <ColumnDefinition />
                <ColumnDefinition
                    Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border
                Background="Transparent"
                Width="40"
                Height="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Border.OpacityMask>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Rectangle
                                Width="35"
                                Height="35"
                                RadiusX="20"
                                RadiusY="20"
                                Fill="Black" />
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Border.OpacityMask>
                <Image
                    Source="{Binding Icon, Converter={StaticResource iconToSource}}"
                    Width="35"
                    Height="35"
                    Margin="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    RenderOptions.BitmapScalingMode="HighQuality" />
            </Border>

            <TextBlock
                Text="{Binding Name}"
                FontFamily="{DynamicResource Roboto}"
                Grid.Column="1"
                FontSize="18"
                Foreground="White"
                VerticalAlignment="Center"
                Margin="10 0" />

            <Button
                Width="34"
                Height="34"
                x:Name="removeButton"
                Style="{DynamicResource ButtonRemoveStyle}"
                Grid.Column="2"
                Visibility="Collapsed"
                VerticalAlignment="Center" Margin="0 0 2 0" />
        </Grid>
    </Grid>
</UserControl>
